#' Plot the progression in pollutant inequality over time
#'
#' @param time_range the years to be used, used for tracking filenames
#' @param pollutant the pollutant being investigated, this should be in quotes and correspond with its name in the data
#' @param name_generator the procedure to generate the input filename, should be a function with year as the imput
#` @keywords faceted, sources
#' @export
#' @examples
#' temporal_inequalities()
#' 

temporal_inequalities <- function(time_range,pollutant,name_generator){
  
  
  
}